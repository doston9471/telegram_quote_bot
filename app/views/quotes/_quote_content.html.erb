<!-- Quote Content Display Partial -->
<!-- Category Badge -->
<div class="mb-4">
  <span class="inline-block bg-gradient-to-r from-purple-600 to-pink-600 text-white text-xs font-bold px-3 py-1.5 rounded-full">
    📂 <%= quote.category %>
  </span>
</div>

<!-- Rich Text Content -->
<div class="prose prose-invert max-w-none mb-6">
  <% if quote.text.present? %>
    <!-- Display the rich text with all its formatting and embedded images -->
    <div class="trix-content space-y-4">
      <%= quote.text %>
    </div>
  <% else %>
    <p class="text-gray-400">No quote text provided.</p>
  <% end %>
</div>

<!-- Author -->
<div class="border-t border-purple-600/30 pt-4">
  <p class="text-gray-400 text-sm font-semibold mb-2">AUTHOR</p>
  <p class="text-lg font-medium text-purple-100">✍️ <%= quote.author %></p>
</div>
